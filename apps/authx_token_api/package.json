{
	"name": "@catalyst/authx_token_api",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "wrangler dev --port 4002 --inspector-port 6002",
		"deploy:uxr": "wrangler deploy --keep-vars --env uxr",
		"deploy:staging": "wrangler deploy --keep-vars --env staging",
		"deploy:preview": "wrangler deploy --keep-vars --env preview",
		"test": "vitest",
		"test:unit": "vitest run --project unit",
		"test:integration": "vitest run --project integration",
		"test:all": "vitest run",
		"test:coverage": "vitest run --coverage",
		"test:coverage:unit": "vitest run --project unit --coverage",
		"test:coverage:integration": "vitest run --project integration --coverage",
		"test:workspace": "vitest run --silent",
		"lint": "eslint . --ext .ts",
		"build": "wrangler build",
		"postinstall": "pnpm build"
	},
	"dependencies": {
		"@catalyst/schema_zod": "workspace:*",
		"@catalyst/schemas": "workspace:*"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.9.9",
		"@cloudflare/workers-types": "^4.20251001.0",
		"@eslint/js": "^9.25.1",
		"@types/uuid": "^9.0.8",
		"@vitest/coverage-istanbul": "^3.2.4",
		"base64url": "^3.0.1",
		"bun": "^1.1.7",
		"jose": "^5.10.0",
		"typescript": "^5.8.3",
		"uuid": "^9.0.1",
		"vitest": "^3.2.4",
		"wrangler": "^4.19.1"
	}
}

{
  "name": "@catalyst-examples/datachannel_tak",
  "scripts": {
    "dev": "npx kill-port 4003 && wrangler dev src/index.ts",
    "deploy": "wrangler deploy --minify src/index.ts",
    "dist": "npx wrangler deploy --dry-run --outdir dist",
    "sim": "pnpm dev",
    "ui:dev": "cd ./datachannel-ui; pnpm start"
  },
  "dependencies": {
    "@apollo/client": "^3.9.9",
    "@graphql-tools/stitching-directives": "^3.0.1",
    "@pothos/core": "^3.41.0",
    "graphql-yoga": "^5.1.1",
    "hono": "^4.0.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240208.0",
    "wrangler": "^3.25.0"
  }
}